# ── Postgres ──
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
POSTGRES_HOST=          # host machine connecting to docker

# ── Redis ──
REDIS_PORT=
REDIS_HOST=

# ── Qdrant ──
QDRANT_PORT=
QDRANT_GRPC_PORT=
QDRANT_HOST=

# ── Adminer ──
ADMINER_PORT=

# ── App (used by FastAPI / Alembic) ──
DATABASE_URL=
SYNC_DATABASE_URL=
REDIS_URL=
QDRANT_URL=
# ── Ingest config ──
ARTICLE_RETENTION_DAYS=
INGEST_INTERVAL_HOURS=


ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_API_KEY=
JWT_SECRET=
JWT_ALGORITHM=
JWT_EXPIRE_MINUTES=


GCP_PROJECT=
GCP_LOCATION=
EMBEDDING_MODEL=
LOG_LEVEL=
TAVILY_API_KEY=

# ── Redis Cache Configurations ──
CACHE_ENABLED=
CACHE_SEARCH_TTL_SECONDS=
CACHE_RAG_TTL_SECONDS=
CACHE_SESSION_TTL_SECONDS=
CACHE_TRENDING_ENABLED=
CACHE_TRENDING_TTL_SECONDS=

# ── Qdrant Configurations ──
QDRANT_COLLECTION=

# ── Embeddings & Chunking ──
EMBEDDING_DIMENSIONS=
CHUNK_SIZE=
CHUNK_OVERLAP=

# ── Hybrid Search ──
HYBRID_SEMANTIC_LIMIT=
HYBRID_BM25_LIMIT=
HYBRID_RESULT_LIMIT=
RRF_K=6

# ── Reranker configurations ──
RERANKER_ENABLED=
RERANKER_MODEL=
RERANK_CANDIDATE_LIMIT=

# ── LLM Token Limit ──
GEMINI_MODEL=
LLM_MAX_OUTPUT_TOKENS=

# ── Speech-To-Text configurations ──
STT_ENABLED=
STT_MODEL=
STT_MAX_AUDIO_BYTES=
STT_MAX_OUTPUT_TOKENS=

# ── Web Fallback configurations (Tavily) ──
WEB_FALLBACK_ENABLED=
WEB_FALLBACK_STALE_HOURS=
WEB_FALLBACK_MIN_HITS=
WEB_FALLBACK_MAX_RESULTS=
WEB_FALLBACK_SEARCH_DEPTH=
WEB_FALLBACK_MIN_SEMANTIC_SCORE=
WEB_FALLBACK_CACHE_TTL_SECONDS=

# ── Celery Ingest / Retention Scheduler ──
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=
SCHEDULER_ENABLED=
RETENTION_CRON_HOUR=
RETENTION_CRON_MINUTE=

# ── Host / Port ──
API_HOST=
API_PORT=

# ── RAG limits and thresholds ──
CONTEXT_CHUNK_LIMIT=
BACKGROUND_SEARCH_LIMIT=
SEMANTIC_SIMILARITY_THRESHOLD=
BM25_RELEVANCE_THRESHOLD=
RELEVANCE_SCORE_FLOOR=